currentTime_Str = input("What is the current time (in hours 0-23)?")
wait_Time_Str = input("How many hours do you want to wait")

currentTimeInt = int(currentTime_Str)
waitTimeInt = int(wait_Time_Str)

finalTime_Int = currentTimeInt + waitTimeInt
print(finalTime_Int)


# I changed the wording for multiple of the words. I made the phrases match each other since each wording was different.
#I changed the wording for currentTimeInt, waitTimeInt, and others. I also closed a parenthesis.